import { generateRequest } from '@/lib/fetch'

export const requestGetDetailPost = async ({}: { authorId: string }) => {
    // return generateRequest({
    //     url: `/api/post/${id}?${searchParams.toString()}`,
    // })
}
