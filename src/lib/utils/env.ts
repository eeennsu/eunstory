export const assertValue = (value?: any, errorMessage: string = 'Environment variable is not defined') => {
    // if (value === undefined) {
    //     throw new Error(errorMessage)
    // }

    return value
}
