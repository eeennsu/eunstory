import { LoginForm } from '@/features/home'
import { FC } from 'react'

const HomePage: FC = () => {
    return (
        <main className='page-container'>
            HomePage
            <LoginForm />
        </main>
    )
}

export default HomePage
